services:
  database:
    image: postgres:17
    restart: always
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    ports:
      - '5432:5432'
    volumes:
      - pg_data:/var/lib/postgresql/data

  music:
    build: .
    ports:
      - '8000:8000'
    env_file: 
      - .env
    depends_on:
      - database
    volumes:
      - .:/app

  celery:
    build: .
    command: /opt/venv/bin/celery -A config worker --loglevel=info
    env_file:
      - .env
    depends_on:
      - database
      - music
    volumes:
      - .:/app

volumes:
  pg_data: